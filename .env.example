# Token que exige el endpoint /track. Sin el, el servidor no arranca.
API_TOKEN=cambia-esto-por-un-valor-largo-y-aleatorio

# Opcionales
PORT=3000
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable
NAV_TIMEOUT_MS=25000
